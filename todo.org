- accessor utility functions in Pool to handle both PdfFile TxtFile objects with
  a unified interface (this is esp. helpful to handle files with and without
  pages)
- maybe add a TxtPage class (even though there will just be one page per file)
  in order to unify file handling
  - also add ABC
  - use page_label = None to signify that no page_label is to be used
- use pypdf instead of pypdf2
- maybe create a Fragment or TextFragment class
  - reference to PoolItem
  - PoolItem data index
  - source page_label
  - text (could be anything; word/token, sentence)
    - should be a spaCy token or similar (this facilitates further extraction
      of data from e.g. the token)
